"""Azure SQL read-only query tool.

Connects to an Azure SQL database using Azure Active Directory interactive
authentication (browser sign-in), runs a single read-only query, and writes
the results to a CSV file.
"""
